// dsh-chat-minimap host half: the feature is client-only; this entry exists so
// the bundle patch has a valid Node-side row to mount.
export const name = 'dsh-chat-minimap'

export function apply() {}
